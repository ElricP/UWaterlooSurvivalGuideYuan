using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CombatControlPanel : MonoBehaviour {

	//public RewardPanel rewardPanel;
	//public GameObject controlPanel;
	//public PlayerController player;
	/*
	// Use this for initialization
	void Start () {
		//rewardPanel.gameObject.SetActive (false);
		controlPanel.SetActive (true);
		GameObject.Find ("rewardPanel").SetActive(false);
		gameObject.SetActive (true);
	}
	
	// Update is called once per frame
	void Update () {
		if (GameObject.Find("player").GetComponent<PlayerController>().currentHealth <= 0) {
			GameObject.Find("player").GetComponent<PlayerController>().speed = 0;
			GameObject.Find("player").GetComponent<PlayerController>().attackSpeed = 0;
			controlPanel.SetActive (false);
			rewardPanel.gameObject.SetActive (true);
			rewardPanel.Reward ();
		}
	}
	*/
}
